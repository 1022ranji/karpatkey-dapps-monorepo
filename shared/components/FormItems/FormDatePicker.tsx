import * as React from 'react'

// import {TFormProps} from "@karpatkey-monorepo/reports/src/types";

const FormDatePicker = () => {
  return (
    <div />
    // <Controller
    //   control={control}
    //   name={name}
    //   render={({
    //              field: {onChange, onBlur, value, name, ref},
    //              fieldState: {invalid, isTouched, isDirty, error},
    //              formState,
    //            }) => (
    //     <DatePicker
    //       inputRef={ref}
    //       label="Basic example"
    //       value={value}
    //       onChange={onChange}
    //       format="YYYY/MM/DD"
    //       renderInput={(params: any) => <TextField onBlur={onBlur} name={name} {...params} error={!!error}
    //                                                helperText={error?.message}/>}
    //       desktopModeMediaQuery="@media (pointer: coarse)"
    //     />
    //   )}
    // />
  )
}

export default FormDatePicker
